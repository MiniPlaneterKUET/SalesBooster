<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.electroscholars.manash.salesboostercomplete"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- <activity android:name=".MainActivity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN"/> -->


        <!-- <category android:name="android.intent.category.LAUNCHER"/> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <!-- <activity android:name=".Authentication.LogInActivity"> -->
        <!-- </activity> -->
        <!-- <activity android:name=".Splash.SplashActivity"> -->
        <!-- </activity> -->
        <!--  -->

        <activity android:name=".Authentication.LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".Splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".TargetVsSales.TargetVsSalesActivity">
        </activity>
        <activity android:name=".News.NewsActivity">
        </activity>
        <activity android:name=".TakeOrder.TakeOrderClientActivity">
        </activity>
        <activity android:name=".TakeOrder.AddItemActivity">
        </activity>
        <activity android:name=".TakeOrder.OrderSubmissionActivity">
        </activity>
        <activity android:name=".Message.MessageActivity">
        </activity>
        <activity android:name=".ViewClient.ClientViewActivity">
        </activity>
    </application>

</manifest>